package base.search;

import org.openqa.selenium.WebDriver;

/**
 * Responsys Search Tools
 */
public class ResSearchTools extends SearchTools {
    public ResSearchTools(WebDriver driver) {
        super(driver);
    }
}
